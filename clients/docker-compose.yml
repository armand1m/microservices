version: '2'
services:
  service:
    build:
      context: ./service
      dockerfile: Dockerfile
    command: npm start
    depends_on:
      - database
    links:
      - database
    volumes:
      - ./service:/src
    ports:
      - "3000"
    environment:
      - NODE_ENV=production
      - DB_PORT=28015
      - PORT=3000

  database:
    image: rethinkdb:latest
    volumes:
      - ./data:/data
